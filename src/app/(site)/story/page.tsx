import StoryContent from '@/components/story/StoryContent'

export const metadata = {
  title: 'Our Story',
  description: 'The story behind Anagrama Art in Knots — Ana, the name, and the animals.',
}

export default function StoryPage() {
  return <StoryContent />
}
